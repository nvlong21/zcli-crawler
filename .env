# --- Application Settings ---
# Generated by fastapi-ddd-generator
PROJECT_NAME=Crawler
ENVIRONMENT=development # Options: development, testing, staging, production
API_V1_STR=/api/v1
# LOG_LEVEL=DEBUG # Uncomment to override default logging level

# --- Security ---
# !!! CHANGE THIS IN PRODUCTION !!! - Generate using: openssl rand -hex 32
SECRET_KEY=change_me_f7dd00d98518c8e9a293147fa9959997
ACCESS_TOKEN_EXPIRE_MINUTES=30 # Optional: Override default token expiry

# --- Database ---
# Select *one* DATABASE_URL based on your chosen database
# PostgreSQL (Sync Example):
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/Crawler_db
# PostgreSQL (Async Example - Recommended):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/Crawler_db
# SQLite (Sync Example - Relative Path):
# DATABASE_URL=sqlite:///./Crawler.db
# SQLite (Async Example - Relative Path - Default):
DATABASE_URL=sqlite+aiosqlite:///./Crawler.db
# Optional DB Pool Settings (used by infrastructure/database/session.py)
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10

# --- Cache ---
CACHE_TYPE=memory # Options: memory, redis
# Redis Settings (only needed if CACHE_TYPE=redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_redis_password # Uncomment and set if Redis requires auth
# REDIS_URL=redis://user:password@host:port/db # Alternative URL format (takes precedence)
# REDIS_MAX_CONNECTIONS=10 # Optional: Pool size

# --- CORS ---
# Comma-separated list of allowed origins. Use '*' for development ONLY.
# Be specific in production, e.g., https://yourfrontend.com,https://www.yourfrontend.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080
# CORS_ORIGINS=*

# --- External Services (Example Placeholders) ---
# Adjust URLs and credentials as needed
EMAIL_SERVICE_URL=grpc://localhost:50051
EMAIL_SERVICE_TOKEN=replace_with_real_email_service_token
EMAIL_SENDER_ADDRESS=noreply@example.com

PAYMENT_SERVICE_URL=http://localhost:8001
PAYMENT_API_KEY=replace_with_real_payment_api_key

NOTIFICATION_SERVICE_URL=http://localhost:8002
NOTIFICATION_API_KEY=replace_with_real_notification_api_key

